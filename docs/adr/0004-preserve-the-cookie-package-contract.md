# Preserve the Cookie package contract

The refactored extension will preserve existing source IDs, `by-service/*.cookies.txt` paths, and Netscape Cookie contents so current Media Dock releases can import new packages without changes. New metadata may be added without changing existing meanings, allowing future Media Dock versions to use richer information while older versions continue to work.
