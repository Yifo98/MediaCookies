# Target Chrome and Edge first

MediaCookies will treat Chrome and Microsoft Edge as first-class browsers, with other Chromium browsers supported on a best-effort basis. Firefox and Safari are outside this refactor because their extension and permission differences would expand the architecture and test matrix without improving the primary Media Dock workflow.
