# Split quick export and export workbench

MediaCookies will use a compact quick-export surface for the frequent path and a full export workbench for scanning, inspection, selection, and multi-source export. A single browser popup cannot present both paths clearly within its constrained viewport; separating them keeps the common task focused while the workbench remains purpose-built for Cookie package export.
