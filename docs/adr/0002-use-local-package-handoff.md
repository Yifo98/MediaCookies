# Use local package handoff

MediaCookies and Media Dock will exchange Cookie data through a user-controlled local package instead of a custom protocol or Native Messaging connection. The extra import step preserves a clear privacy boundary, works across supported Chromium browsers, and avoids coupling extension installation to a desktop integration layer.
